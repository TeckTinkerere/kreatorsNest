### Task 7: Documents hub

Create `src/pages/Documents.jsx` — merge Downloads + Templates
Extract `DownloadCard` to `src/components/DownloadCard.jsx` from `src/pages/Downloads.jsx` if needed

Use downloadsData + resourceData filter type Templates. Category pills. ResourceCard full variant for templates.

Commit: `feat(pages): add merged Documents hub`

Do NOT add route in App yet if Task 8 handles routes — but page must be importable.
